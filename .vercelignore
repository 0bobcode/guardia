# These are separate mobile app projects, not part of the Next.js
# deployment — their node_modules/Pods/build artifacts blow way past
# Vercel's upload file-count limit if not excluded.
mobile-companion/
android-companion/
ios-companion/

# Stray local recordings (e.g. screen recordings used as design references)
# — never meant to be deployed, and can exceed Vercel's 100MB file limit.
*.mov
